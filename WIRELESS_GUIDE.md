# 📡 دليل التوصيل اللاسلكي (Wireless ADB)

## 🎯 نظرة عامة

يتيح لك Android Studio Lite الاتصال بأجهزة Android لاسلكيًا، مما يُغنيك عن كابل USB ويوفر حرية أكبر في التطوير والاختبار.

### ✨ المميزات الجديدة

- **💾 Auto-Save**: حفظ الأجهزة اللاسلكية تلقائيًا
- **🔄 Auto-Reconnect**: إعادة الاتصال التلقائي عند بدء VS Code
- **🔧 Smart Detection**: اكتشاف تلقائي لنوع الاتصال (Wireless Debugging vs TCP/IP)
- **📋 Context Menu**: إدارة سهلة للأجهزة من قائمة النقر بزر الماوس الأيمن

---

## 🛠️ طريقتان للاتصال

### 1️⃣ Wireless Debugging (Android 11+)

**الأسهل - لا يحتاج كابل USB على الإطلاق!**

#### المتطلبات:
- Android 11 أو أحدث
- الجهاز والكمبيوتر على نفس شبكة WiFi

#### خطوات الإعداد:

##### على الجهاز (المرة الأولى فقط):
1. اذهب إلى **Settings** → **About phone**
2. اضغط على **Build number** 7 مرات لتفعيل Developer options
3. ارجع للإعدادات واذهب إلى **Developer options**
4. فعّل **Wireless debugging**

##### في VS Code:
1. في الشريط الجانبي **Android**، اذهب لقسم **📡 Wireless Devices**
2. اضغط على **➕ Add Wireless Device**
3. اختر **$(radio-tower) Wireless Debugging**
4. اختر **$(key) Pairing Code**

##### على الجهاز مرة أخرى:
5. في شاشة **Wireless debugging**، اضغط **Pair device with pairing code**
6. سيظهر:
   - **WiFi pairing code**: مثال `123456`
   - **IP address & Port**: مثال `192.168.1.100:45678`

##### في VS Code:
7. أدخل **IP:Port** (مثال: `192.168.1.100:45678`)
8. أدخل **Pairing Code** المكون من 6 أرقام (مثال: `123456`)
9. انتظر رسالة "تم الإقران بنجاح"
10. على الجهاز، ارجع لشاشة **Wireless debugging** الرئيسية
11. ستجد Port آخر تحت "Device name" (مثال: `37843`)
12. أدخله في VS Code
13. ✅ تم! الآن الجهاز متصل لاسلكيًا

> [!TIP]
> **لا تحتاج لتكرار الإقران!** 
> - بعد الإقران الأول، سيُحفظ الجهاز تلقائيًا
> - عند إعادة تشغيل VS Code، سيتصل الجهاز تلقائيًا
> - فقط تأكد أن Wireless debugging مُفعّل على الجهاز

---

### 2️⃣ ADB over TCP/IP (Android 4.0+)

**للأجهزة القديمة - يحتاج كابل USB لمرة واحدة فقط**

#### المتطلبات:
- Android 4.0 أو أحدث
- كابل USB (للإعداد الأولي فقط)
- الجهاز والكمبيوتر على نفس شبكة WiFi

#### خطوات الإعداد:

##### الطريقة A: جهاز متصل بـ USB حاليًا

1. وصّل الجهاز بكابل USB
2. في VS Code، اضغط **➕ Add Wireless Device**
3. اختر **$(plug) ADB over TCP/IP**
4. اختر **$(usb) جهاز متصل عبر USB**
5. اختر الجهاز من القائمة
6. سيقوم البرنامج تلقائيًا بـ:
   - الحصول على IP الجهاز
   - تفعيل TCP/IP mode
   - عرض عنوان الاتصال (مثال: `192.168.1.100:5555`)
7. اضغط **اتصال الآن ✅**
8. ✅ افصل كابل USB - الجهاز الآن متصل لاسلكيًا!

##### الطريقة B: جهاز تم إعداده مسبقًا

1. اضغط **➕ Add Wireless Device**
2. اختر **$(plug) ADB over TCP/IP**
3. اختر **$(globe) جهاز على الشبكة**
4. اختر:
   - **$(edit) إدخال IP يدوياً**: إذا كنت تعرف IP الجهاز
   - **$(search) مسح الشبكة**: للبحث التلقائي (قد يستغرق وقتًا)
5. أدخل IP الجهاز (مثال: `192.168.1.100`)
6. ✅ تم الاتصال!

> [!NOTE]
> **كيف أعرف IP جهازي؟**
> 
> على الجهاز: **Settings** → **About phone** → **Status** → **IP address**

---

## 🎛️ إدارة الأجهزة اللاسلكية

### عرض الأجهزة المتصلة

في الشريط الجانبي **Android**، قسم **📡 Wireless Devices**:

```
📡 Wireless Devices
   ➕ Add Wireless Device
   📡 Realme 6 Pro    192.168.1.100:37843 (wireless-debug)
   🔌 Galaxy S10      192.168.1.101:5555 (tcpip)
```

**رموز نوع الاتصال:**
- **📡**: Wireless Debugging (Android 11+)
- **🔌**: TCP/IP (Port 5555)

### Context Menu (النقر بزر الماوس الأيمن)

اضغط بزر الماوس الأيمن على أي جهاز لاسلكي:

- **🔄 Reconnect**: إعادة الاتصال يدويًا
- **🗑️ Forget Device**: نسيان الجهاز (لن يتصل تلقائيًا بعد الآن)
- **❌ Disconnect**: قطع الاتصال الحالي

---

## 🔄 Auto-Reconnect

### كيف تعمل؟

1. **Auto-Save**: عند الاتصال بجهاز لاسلكي، يُحفظ تلقائيًا
2. **Auto-Reconnect**: عند بدء VS Code، تُحاول الإضافة إعادة الاتصال بجميع الأجهزة المحفوظة
3. **Parallel Connection**: إعادة الاتصال بجميع الأجهزة بالتوازي (سريع!)

### في الـ Console

افتح **Developer Tools** (Help → Toggle Developer Tools):

```
🔄 Attempting to reconnect 2 saved devices...
✅ Reconnected: 192.168.1.100:37843
⚠️ Failed to reconnect 192.168.1.101:5555
✅ Reconnected 1 device(s)
⚠️ Failed to reconnect 1 device(s)
```

> [!IMPORTANT]
> **لماذا فشل الاتصال؟**
> - الجهاز مُطفأ أو غير متصل بالشبكة
> - Wireless debugging غير مُفعّل على الجهاز
> - تغيّر IP الجهاز (استخدم DHCP Reservation في الراوتر)

---

## 🐛 حل المشاكل (Troubleshooting)

### ❌ "فشل الإقران"

**الأسباب الشائعة:**

1. **IP أو Port خاطئ**
   - ✅ تأكد من نسخ IP و Port بدقة من الجهاز
   - ✅ لا تخلط بين Pairing Port و Connection Port

2. **الشبكة غير متطابقة**
   - ✅ تأكد أن الجهاز والكمبيوتر على نفس WiFi
   - ❌ لا تستخدم VPN أثناء الإعداد

3. **Pairing Code انتهى**
   - ✅ Code صالح لفترة محدودة - جرّب مرة أخرى
   - ✅ لا تُغلق شاشة Pairing على الجهاز حتى تنتهي

### ❌ "لا يتصل تلقائيًا"

**الحلول:**

1. **تأكد من حفظ الجهاز**:
   - يجب أن يكون `state: 'device'` عند الحفظ
   - افحص Console: `💾 Saved 1 wireless devices`

2. **تفعيل Wireless debugging على الجهاز**:
   - يجب تفعيله كل مرة بعد إعادة تشغيل الجهاز
   - بعض الأجهزة تُطفئه تلقائيًا بعد فترة

3. **IP تغيّر**:
   - استخدم **DHCP Reservation** في إعدادات الراوتر
   - أو: احذف الجهاز القديم (**Forget**) وأعد الاتصال

### ❌ "الاتصال بطيء"

**التحسينات:**

1. **تأكد من قوة إشارة WiFi**
2. **استخدم WiFi 5GHz** إذا متوفر (أسرع من 2.4GHz)
3. **أوقف VPN** على الجهاز أو الكمبيوتر
4. **أعد تشغيل الراوتر** إذا كانت المشكلة مستمرة

---

## 💡 نصائح وحيل

### إعداد سريع مع DHCP Reservation

لضمان بقاء IP ثابت للجهاز:

1. اذهب لإعدادات الراوتر (عادة `192.168.1.1`)
2. ابحث عن **DHCP Reservation** أو **Static IP**
3. اربط **MAC Address** الجهاز بـ IP محدد
4. الآن Auto-reconnect سيعمل دائمًا! 🎉

### استخدام SSH Tunnel (Advanced)

للاتصال من شبكة مختلفة:

```bash
# على جهاز في نفس شبكة الـ Android
ssh -L 5555:192.168.1.100:5555 user@remote-server

# في VS Code على كمبيوتر آخر
adb connect localhost:5555
```

### مسح جميع الأجهزة المحفوظة

إذا أردت البدء من الصفر:

1. افتح **Command Palette** (Ctrl+Shift+P)
2. ابحث عن: `Developer: Open User Settings (JSON)`
3. ابحث عن `android.wirelessDevices` واحذفه
4. أعد تشغيل VS Code

---

## 📊 مقارنة الطرق

| الميزة | Wireless Debugging | TCP/IP |
|--------|-------------------|---------|
| **Android Version** | 11+ | 4.0+ |
| **يحتاج USB** | ❌ لا | ✅ مرة واحدة فقط |
| **سهولة الإعداد** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **الأمان** | 🔐 Pairing مُشفّر | ⚠️ غير مُشفّر |
| **Port** | عشوائي (>30000) | 5555 ثابت |
| **سرعة الاتصال** | سريع | سريع |
| **Auto-Reconnect** | ✅ يعمل | ✅ يعمل |

---

## 🔒 ملاحظات أمنية

> [!CAUTION]
> **Wireless Debugging تأمين مهم:**
> 
> - لا تترك Wireless debugging مُفعّلاً على شبكة عامة (مثل WiFi المقاهي)
> - استخدم شبكة خاصة أو VPN
> - أطفئ الميزة عند عدم الحاجة إليها

> [!WARNING]
> **TCP/IP Mode غير مُشفّر:**
> 
> - أي شخص على نفس الشبكة يمكنه الاتصال بجهازك
> - استخدم شبكة موثوقة فقط
> - فكّر في استخدام Firewall على الجهاز

---

## 📚 موارد إضافية

- [Android Developer Docs - Wireless Debugging](https://developer.android.com/studio/command-line/adb#wireless)
- [ADB Commands Reference](https://developer.android.com/studio/command-line/adb)

---

## 🆘 الحصول على مساعدة

إذا واجهت مشكلة:

1. **افحص Console Logs**: Help → Toggle Developer Tools → Console
2. **افحص ADB Connection**: افتح Terminal وجرّب `adb devices`
3. **أعد تشغيل ADB Server**:
   ```bash
   adb kill-server
   adb start-server
   ```

---

**آخر تحديث**: 2026-01-20  
**الإصدار**: 0.1.0 مع Persistence Support 🎉
